{
  "name": "openbrush-website",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "export": "next export",
    "start": "next start",
    "lint": "next lint",
    "deploy": "next build && next export && git add out/ && git commit -m \"Update build for gh-pages\" && git fetch && git checkout gh-pages && git checkout development -- out && git add out/ && git commit -m \"Deploy gh-pages\" && git push origin gh-pages"
  },
  "dependencies": {
    "next": "11.1.0",
    "prettier": "^2.3.2",
    "prismjs": "^1.24.1",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "react-syntax-highlighter": "^15.4.4",
    "sass": "^1.38.0"
  },
  "devDependencies": {
    "eslint": "7.32.0",
    "eslint-config-next": "11.1.0"
  }
}
